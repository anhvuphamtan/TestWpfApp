<UserControl x:Class="Jarvis_Windows.Sources.MVVM.Views.JarvisActionView.JarvisActionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Jarvis_Windows.Sources.MVVM.Views.JarvisActionView"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
            xmlns:media="clr-namespace:System.Windows.Media;assembly=PresentationCore"
            xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
            xmlns:animations="clr-namespace:System.Windows.Media.Animation;assembly=PresentationCore"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="20">
    <UserControl.Resources>
        
        <Style x:Key="ImageButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Width" Value="22"/>
                                <Setter Property="Height" Value="22"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
        <Button Style="{StaticResource ImageButtonStyle}"
                Command="{Binding ShowMenuOperationsCommand}"
                IsEnabled="{Binding IsAnimationEnabled}">
            <!--<Image Source="../../../../Assets/Images/jarvis_logo.png"/>-->
            <Image x:Name="img" Source="../../../../Assets/Images/jarvis_logo.png">
                <Image.RenderTransform>
                    <RotateTransform x:Name="rotateTransform" Angle="0"/>
                </Image.RenderTransform>
                <Image.RenderTransformOrigin>
                    <Point X="0.5" Y="0.5"/>
                </Image.RenderTransformOrigin>
                <!--<Image.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetName="rotateTransform"
                                    Storyboard.TargetProperty="Angle"
                                    To="360"
                                    Duration="0:0:1"
                                    RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    --><!--<EventTrigger RoutedEvent="MouseLeave">
                        <StopStoryboard BeginStoryboardName="rotateStoryboard"/>
                    </EventTrigger>--><!--
                </Image.Triggers>-->
            </Image>
        </Button>
    </Border>
</UserControl>
